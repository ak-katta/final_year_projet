# Copy to .env and fill in. Never commit .env.
# Groq keys: https://console.groq.com/keys      (look like gsk_...)
# Gemini keys: https://aistudio.google.com/apikey (look like AIza...)
# Numbered suffixes _1.._3 are optional; each becomes a failover provider.
GROQ_API_KEY_1=
GEMINI_API_KEY_1=
